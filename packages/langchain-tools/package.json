{
  "name": "@fs-based-agent/langchain-tools",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "clean": "tsc -b --clean && node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc -b"
  },
  "license": "MIT",
  "dependencies": {
    "glob": "^11.0.3"
  },
  "peerDependencies": {
    "@langchain/core": "^1.0.0",
    "zod": "^3.0.0 || ^4.0.0"
  }
}
